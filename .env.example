# SpreadConnect Client Environment Variables
# Copy this file to .env and fill in your actual values
# Never commit the .env file to version control

# Required: SpreadConnect API access token
# Get this from your SpreadConnect account/dashboard
SPREAD_CONNECT_ACCESS_TOKEN=your-real-api-key-here

# Optional: Override the default SpreadConnect API endpoint
# Default is https://api.spreadconnect.app
# SPREAD_CONNECT_BASE_URL=https://api.spreadconnect.app

# To use these variables:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your real API key
# 3. Load variables: source .env
# 4. Run your commands: mix import.csv orders.csv

# Note: Test environment doesn't require these variables
# Tests use safe fake credentials automatically